# CI-Compatible Requirements - Updated for reliable installation
# This version prioritizes stability and pre-compiled wheels

# Core Django dependencies
Django==5.2.5
djangorestframework==3.15.2
django-cors-headers==4.4.0
psycopg[binary]==3.2.3
Pillow==10.4.0

# Document processing - reliable packages
PyPDF2==3.0.1
python-docx==1.1.2
pdfplumber==0.9.0

# Core scientific computing (using stable versions)
numpy==1.26.4

# Computer vision - headless version for servers
opencv-python-headless==4.8.1.78

# Machine learning - using older stable version
scikit-learn==1.3.2

# OCR
pytesseract==0.3.13

# Text processing
nltk==3.9.1
textblob==0.18.0

# Build essentials
setuptools>=75.1.0
wheel>=0.44.0

# Note: scipy removed to avoid compilation issues
# If scipy is absolutely needed, install it separately:
# pip install --only-binary=all scipy

# For matplotlib (optional):
# pip install --only-binary=all matplotlib
