# CI/CD Optimized Requirements
# This file prioritizes prebuilt wheels and faster installation for CI environments

# Core Django dependencies - use exact versions for CI stability
Django==5.2.5
djangorestframework==3.15.2
django-cors-headers==4.4.0
psycopg[binary]==3.2.3
Pillow==10.4.0

# AI Document Verification Dependencies - CI Optimized
PyPDF2==3.0.1
# PyMuPDF==1.24.10  # Commented out - causes build issues on Windows CI
python-docx==1.1.2
pdfplumber==0.9.0  # Alternative to PyMuPDF for PDF processing

# Use headless OpenCV for CI environments (smaller, no GUI dependencies)
opencv-python-headless==4.10.0.84

# Scientific computing - using wheels when available
# Install these individually to avoid build conflicts
numpy>=1.21.0,<2.0.0

# Text processing - core dependencies only
pytesseract==0.3.13

# Build tools (ensure wheels can be built if needed)
setuptools>=75.1.0
wheel>=0.44.0
pip>=24.0

# WSGI server for production
gunicorn>=20.1.0

# Note: scipy, scikit-learn, and PyMuPDF moved to separate installation step
# to avoid compilation issues in CI environments
