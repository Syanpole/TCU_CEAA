# Core Django dependencies - use exact versions for CI stability
Django==5.2.5
djangorestframework==3.15.2
django-cors-headers==4.4.0
psycopg[binary]==3.2.3

# Image processing - ensure wheel availability
Pillow==10.4.0

# Document processing - lightweight versions for CI
PyPDF2==3.0.1
python-docx==1.1.2
pdfplumber==0.9.0

# AI/ML dependencies - using wheel-compatible versions
numpy==1.26.4
# scikit-learn==1.5.2  # Temporarily disabled - causes SciPy compilation issues in CI
opencv-python-headless==4.10.0.84
pytesseract==0.3.13

# Text processing - optional but useful
# nltk==3.9.1          # Commented out - requires data downloads
# textblob==0.18.0     # Commented out - requires NLTK data

# Build tools for CI compatibility - ensure latest versions
setuptools>=75.1.0
wheel>=0.44.0
pip>=24.0
