# ULTRA-SAFE CI REQUIREMENTS - WHEEL-ONLY INSTALLATION
# These packages are guaranteed to install without C++ compilation

# Core Django dependencies (always wheel-compatible)
Django==5.2.5
djangorestframework==3.15.2
django-cors-headers==4.4.0
Pillow==10.4.0

# Database (using binary version to avoid compilation)
psycopg[binary]==3.2.3

# Document processing - pure Python or wheel-only
PyPDF2==3.0.1
python-docx==1.1.2
pdfplumber==0.9.0

# Essential AI/ML - wheel-compatible versions only
numpy==1.26.4
opencv-python-headless==4.10.0.84

# Text processing - pure Python packages
pytesseract==0.3.13
nltk==3.9.1
textblob==0.18.0

# Build tools
setuptools>=75.1.0
wheel>=0.44.0
pip>=24.0

# Note: scikit-learn and scipy are INTENTIONALLY EXCLUDED
# They require compilation and cause CI failures
# Use numpy for basic math operations
# Use simpler alternatives for ML if needed in CI
