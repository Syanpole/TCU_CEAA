# PRODUCTION-READY CI REQUIREMENTS
# This file contains only stable packages that install reliably in CI/CD environments
# Tested with GitHub Actions on Ubuntu latest

# Core Django framework
Django==5.2.5
djangorestframework==3.15.2
django-cors-headers==4.4.0

# Database connector (binary package - no compilation)
psycopg[binary]==3.2.3

# Image processing
Pillow==10.4.0

# Document processing (pure Python, no compilation issues)
PyPDF2==3.0.1
python-docx==1.1.2

# Core scientific computing
numpy==1.26.4

# Text processing (lightweight)
nltk==3.9.1

# Build tools
setuptools>=75.1.0
wheel>=0.44.0
pip>=24.0

# The following packages are commented out as they can cause CI issues:
# Install them manually if needed:
#
# opencv-python-headless==4.8.1.78  # Computer vision
# scikit-learn==1.3.2               # Machine learning
# scipy==1.11.4                     # Scientific computing (causes OpenBLAS issues)
# matplotlib==3.8.4                 # Plotting
# pytesseract==0.3.13               # OCR
# textblob==0.18.0                  # Text processing
# pdfplumber==0.9.0                 # Advanced PDF processing

# To install optional packages after main installation:
# pip install --only-binary=all opencv-python-headless scikit-learn
# pip install pytesseract textblob pdfplumber
